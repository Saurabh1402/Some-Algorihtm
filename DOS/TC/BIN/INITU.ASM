ASSUME CS:CODE,DS:DATA

DATA SEGMENT
OPR1 DW 1234h
OPR2 DW 01 DUP(?)
DATA ENDS
CODE SEGMENT
START: 	MOV AX, DATA
	MOV DS, AX
	MOV AX, OPR1
	MOV BX, OPR2
	CLC
	ADD AX, BX
	MOV DI, RESULT
	MOV [DI],AX
	MOV AH, 4CH
	INT 21H
CODE ENDS
     END START